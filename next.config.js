/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    unoptimized: true, // disable Next.js image optimization (optional for simplicity)
  },
};

module.exports = nextConfig;
